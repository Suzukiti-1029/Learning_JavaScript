let options = true;
if (!options) options = {};
console.log(options);

options = options || {};
console.log(options);

options = false;
if (!options) options = {};
console.log(options);

options = options || {};
console.log(options);
